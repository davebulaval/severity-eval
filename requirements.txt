# severity-eval — core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
scikit-learn>=1.3       # cohen_kappa_score in validate_severity_llm.py

# HuggingFace
evaluate>=0.4
datasets>=2.14
transformers>=4.45
huggingface_hub>=0.20

# API clients
openai>=1.0             # also used for xAI via custom base_url
anthropic>=0.20
google-generativeai>=0.3   # single-call path (call_google)
google-genai>=1.0          # batch API path (_evaluate_batch_google)
mistralai>=0.1
cohere>=5.0
httpx>=0.27             # xAI batch API HTTP client

# Experiment pipeline
python-dotenv>=1.0
wandb>=0.16
tqdm>=4.60

# Local-model inference (vLLM only). PagedAttention + continuous
# batching + prefix caching. Pulls ~2-3 GB of CUDA kernels.
# See experiments/VLLM_BACKEND.md for install notes.
torch>=2.1
vllm>=0.6
bitsandbytes>=0.43      # used by vLLM for the unsloth/-bnb-4bit quants

# Dev / testing / styling
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4
