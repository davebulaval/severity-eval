# Minimal deps for CI test runs.
#
# The full requirements.txt pulls torch, unsloth, bitsandbytes etc. which
# are 3+ GB and require CUDA -- the GitHub Actions ubuntu-latest runners
# have no GPU. These tests cover only the pure-logic helpers (scoring,
# risk measures, taxonomy, sensitivity, bench_inference helpers, vLLM
# helpers without the engine), so we only need the lightweight stack.
#
# The HF-conditional tests (test_metric.py) skip themselves when
# evaluate / datasets are not installed, which is the case here.

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
python-dotenv>=1.0

# Testing
pytest>=7.0
pytest-cov>=4.0
